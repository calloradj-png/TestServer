# Test Project — Game Server

Standalone authoritative game server built with DreamEngine.

## Quick Start
```bash
npm install
npm start
```

## Docker
```bash
docker build -t test_project-server .
docker run -p 3003:3003 test_project-server
```
